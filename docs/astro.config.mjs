// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
    // Replace this with your actual GitHub username and repository name!
    site: 'https://fongajorge.github.io',
    base: 'SG1_Team6/',
});